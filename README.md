# gtk-example

# Setup
```bash
# build
make || make all

# build && test
make test

# generate compile_commands.json for clangd lsp in vim/nvim
make compile_commands

# clean build directory
make clean
```
